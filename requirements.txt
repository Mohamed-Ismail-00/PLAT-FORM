# Core Framework
fastapi>=0.115.0

# Database
sqlalchemy[asyncio]>=2.0.31
asyncpg>=0.30.0
aiosqlite>=0.20.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.3

# Validation & Serialization
pydantic>=2.8.0
pydantic-settings>=2.3.0
email-validator>=2.2.0

# Logging
structlog>=24.2.0

# HTTP
httpx>=0.27.0

# Utils
python-multipart>=0.0.9
python-dateutil>=2.9.0

# Data-processing dependencies used only by the seed/import workflow are kept
# in backend/requirements.txt and intentionally excluded from the Vercel
# Serverless Function bundle.
